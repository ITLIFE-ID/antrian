<% content_for(:title) do %>
  <%= "Callers"%>
<% end %>
<div class="card">
  <div class="card-body">
    <%=render "connection_alert_status"%>
    <div class="row">
      <div class="col-8" data-controller="caller"><%=render "caller"%></div>
      <div class="col-4"><%=render "missed_queues", missed_queues: @missed_queues%></div>
    </div>
  </div>
</div>
